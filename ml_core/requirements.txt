# Deep Learning
torch==2.1.0
torchvision==0.16.0
timm==0.9.12  # EfficientNet models

# Data Processing
pandas==2.1.3
numpy==1.24.3
scikit-learn==1.3.2
albumentations==1.3.1
opencv-python==4.8.1.78
pillow==10.1.0

# Visualization
matplotlib==3.8.2
seaborn==0.13.0

# Utilities
tqdm==4.66.1
python-dotenv==1.0.0

# API (Phase 6)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0

# OOD Detection (Phase 4)
faiss-cpu==1.7.4  # Use faiss-gpu if you have CUDA

# Testing
pytest==7.4.3
pytest-cov==4.1.0


Most HAM10000 pipelines fail later because of silent data issues.
Before preprocessing, I strongly recommend verifying:

Missing images vs metadata rows

Duplicate filenames

Corrupt JPEGs

Class imbalance sanity

Case-split leakage (same lesion across train/test)